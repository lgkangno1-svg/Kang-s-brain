import assert from 'node:assert/strict';
import {readFileSync} from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
const root=path.join(path.dirname(fileURLToPath(import.meta.url)),'..');
const read=(p)=>readFileSync(path.join(root,p),'utf8');

const data=read('src/lib/travel/gyeongbokgung-nearby.ts');
const core=read('src/lib/travel/gyeongbokgung-nearby-core.ts');
const content=read('src/lib/content/travel-content.ts');
const explorer=read('src/features/explore/NearbyExplorer.tsx');
const page=read('src/app/[locale]/explore/nearby/page.tsx');
const palace=read('src/app/[locale]/explore/gyeongbokgung/page.tsx');
const quickHelp=read('src/features/quick-help/data.ts');
const seo=read('src/lib/seo/localized-metadata.ts');
const sitemap=read('src/app/sitemap.ts');

assert.match(core,/NearbyFocus='seochon'\|'bukchon'\|'insadong'\|'easy'/,'Nearby routes must expose distinct visitor intents.');
assert.match(core,/NearbyBudget=60\|120\|180/,'Nearby routes must support 1, 2 and 3 hour budgets.');
assert.ok((data.match(/checkedAt:'2026-09-09'/g)??[]).length>=5,'Nearby inventory must keep source-check dates.');
assert.match(data,/checkedAt:'2026-09-11'/,'Rechecked holiday-sensitive records must carry the latest provenance date.');
assert.match(data,/restrictedWindow/,'Nearby data must model time-sensitive visitor restrictions.');
assert.match(data,/Bukchon-ro 11-gil/,'Bukchon restricted-area timing must be explicit rather than generalized to all of Bukchon.');
assert.match(data,/sourceUrl:OFFICIAL/,'Nearby stops must retain official source URLs.');
assert.match(core,/google\.com\/maps\/search/,'Nearby stops must use a zero-API map handoff.');
assert.match(core,/nearbyRouteMapUrl/,'Nearby core must expose a route-level map handoff.');
assert.match(core,/google\.com\/maps\/dir/,'The full-route handoff must use provider-native directions rather than a new routing API.');
assert.match(core,/travelmode:'walking'/,'Nearby route handoff must explicitly request walking directions.');
assert.match(core,/params\.set\('waypoints',waypoints\)/,'Multi-stop Nearby routes must preserve intermediate stops as waypoints.');
assert.match(data,/closedWeekdays:\[6\]/,'Known fixed weekly closures must be modeled explicitly from official source data.');
assert.match(core,/closedDates\?:string\[\]/,'Nearby stop contracts must support source-verified exact closure dates.');
assert.match(core,/stop\.closedDates\?\.includes\(date\)/,'Exact verified closure dates must be evaluated before opening-window logic.');
assert.match(data,/closedDates:\['2026-02-17','2026-09-25'\]/,'The official Lunar New Year and Chuseok closure days verified for 2026 must be modeled exactly.');
assert.match(data,/adjacent holiday days, temporary closures and other years still require a source re-check/,'Unverified holiday extensions and future years must remain fail-closed to a source re-check rather than guessed.');
assert.match(content,/isDateList/,'Headless Nearby validation must schema-check exact closure dates.');
assert.match(content,/closedWeekdays,closedDates,closureNote/,'Optional CMS Nearby records must request the same exact closure-date field as local verified content.');
assert.match(core,/nearbyStopAvailabilityAt/,'Nearby deterministic core must expose date/time availability evaluation.');
assert.match(core,/weekdayFromIso/,'Weekly closure evaluation must derive weekday from the visitor-selected ISO date.');
assert.match(core,/if\(date&&nearbyStopAvailabilityAt\(base,date,720\)==='closed'\)continue/,'Routes must omit stops with a known weekly or exact-date closure on the selected date.');

assert.match(core,/export const NEARBY_WALKING_LEGS/,'Nearby routing must keep explicit source-traceable walking-leg calibration rather than destination-only transfer guesses.');
assert.match(core,/fromId:'gyeongbokgung',toId:'gwanghwamun',minutes:5/,'The palace-to-Gwanghwamun first leg must be calibrated.');
assert.match(core,/fromId:'gyeongbokgung',toId:'seochon',minutes:12/,'The palace-to-Seochon first leg must be calibrated.');
assert.match(core,/fromId:'gyeongbokgung',toId:'bukchon',minutes:15/,'The palace-to-Bukchon first leg must be calibrated.');
assert.match(core,/fromId:'gyeongbokgung',toId:'insadong',minutes:20/,'The palace-to-Insadong first leg must be calibrated.');
assert.match(core,/checkedAt:'2026-09-11',confidence:'medium'/,'Walking calibration must carry checkedAt and confidence provenance.');
assert.match(core,/let previousId='gyeongbokgung'/,'Route budgeting must begin with the actual palace origin rather than zero-cost first arrival.');
assert.match(core,/nearbyWalkingLeg\(previousId,base\.id\)/,'Each transfer must be looked up from the actual previous stop.');
assert.match(core,/if\(!leg\)break/,'Missing walking-leg calibration must fail closed rather than invent a duration.');
assert.match(core,/remaining-=leg\.minutes\+stay/,'Walking time and dwell time must both consume the visitor time budget.');
assert.match(core,/previousId=base\.id/,'The next walking leg must use the stop actually added to the route.');

assert.doesNotMatch(explorer,/useSearchParams/,'Nearby Explorer must not force the functional shell behind a client-only search-param bailout.');
assert.match(explorer,/initialDate\?:string;initialTime\?:string/,'Nearby Explorer must accept validated server-resolved itinerary handoff values.');
assert.match(explorer,/useState\(initialDate\?\?localToday\(\)\)/,'Nearby date state must seed from validated server handoff while preserving a local default.');
assert.match(explorer,/useState\(initialTime\?\?'14:00'\)/,'Nearby time state must seed from validated server handoff.');
assert.match(explorer,/stops:readonly NearbyStop\[\]/,'Nearby Explorer must consume server-resolved content rather than owning the catalog.');
assert.match(explorer,/nearbyRoute\(stops,focus,budget,date\)/,'Nearby Explorer must recalculate routes from server content and explicit visitor choices including date.');
assert.match(explorer,/nearbyRouteMapUrl\(routeStops\)/,'Nearby Explorer must build a single full-route map handoff from the chosen deterministic stops.');
assert.match(explorer,/href=\{routeMap\}/,'The generated full walking route must be directly actionable from the result.');
assert.ok((explorer.match(/fullRoute:'/g)??[]).length===6,'The full-route action must have native copy for all six P0 locales.');
assert.ok((explorer.match(/walkingPolicy:'/g)??[]).length===6,'Walking-estimate limitations and verification guidance must have native copy for all six P0 locales.');
assert.match(explorer,/routeStops\.map\(stop=>\{cursor\+=stop\.transferMinutes;const arrival=cursor/,'Timeline arrival must include the first calibrated walk as well as later transfers.');
assert.doesNotMatch(explorer,/if\(index\)cursor\+=stop\.transferMinutes/,'The first walking leg must never be treated as zero minutes.');
assert.match(explorer,/<strong>\{c\.walk\}:<\/strong> ~\{stop\.transferMinutes\}/,'Every displayed stop, including the first, must disclose its planned walking transfer.');
assert.match(explorer,/nearbyStopAvailabilityAt\(stop,date,stop\.arrival\)/,'Nearby Explorer must evaluate time restrictions against each planned arrival.');
assert.match(explorer,/navigator\.clipboard\?\.writeText/,'Nearby route must be copyable for use during the trip.');
assert.match(explorer,/routeMap\]\.filter\(Boolean\)/,'Copied itinerary text must include the full-route map handoff when available.');
assert.match(explorer,/closurePolicy/,'Nearby Explorer must disclose closure filtering and re-check limits in every P0 locale.');
assert.doesNotMatch(explorer,/gyeongbokgung-nearby';/,'Nearby client must not import the verified local content module.');
assert.doesNotMatch(explorer,/fetch\s*\(/,'Nearby Explorer must remain zero-API.');
assert.doesNotMatch(explorer,/openai|openrouter|anthropic/i,'Nearby Explorer must not depend on an AI provider.');

assert.match(page,/searchParams:Promise/,'The Nearby Server Component must own URL handoff parsing so the functional shell remains server-renderable.');
assert.match(page,/validDate\(one\(query\.date\)\)/,'The Nearby page must validate date handoff input before it reaches deterministic planning.');
assert.match(page,/validTime\(one\(query\.time\)\)/,'The Nearby page must validate time handoff input before it reaches deterministic planning.');
assert.match(page,/getGyeongbokgungNearbyStops\(\)/,'The localized Nearby Server Component must resolve source-checked content.');
assert.match(page,/initialDate=\{validDate\(one\(query\.date\)\)\}/,'The localized Nearby page must pass validated date context into the planner.');
assert.match(page,/initialTime=\{validTime\(one\(query\.time\)\)\}/,'The localized Nearby page must pass validated time context into the planner.');
assert.match(palace,/href="\/explore\/nearby"/,'Gyeongbokgung must continue into Nearby Explorer.');
assert.match(quickHelp,/href:'\/explore\/nearby'/,'Quick Help nearby guidance must open the real Nearby Explorer.');
assert.match(seo,/'\/explore\/nearby'/,'Nearby Explorer must participate in localized canonical/hreflang URLs.');
assert.match(sitemap,/'\/explore\/nearby'/,'Nearby Explorer must be included in the public sitemap.');

console.log('Nearby Explorer contracts passed: source-checked deterministic 1–3h routes, first-leg and pairwise walking calibration, date-aware closures, time restrictions, full walking-route/copy/map handoff, real navigation, and zero AI API.');
